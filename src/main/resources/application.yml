spring:
  application:
    name: car4r
  profiles:
    active: dev
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB


server:
  port: 8080

logging:
  level:
    org:
      springframework:
        web: info

  # config mail
  mail:
    host: smtp.gmail.com
    port: 587
    username: hieuhoctap2004@gmail.com
    password: skgb robb lkva upam
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true